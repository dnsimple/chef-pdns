---
driver:
  name: vagrant

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-14.04
  # - name: ubuntu-12.04
  # - name: bento/centos-7.2

suites:
  - name: recursor
    run_list:
      - recipe[test::default]
    attributes:
