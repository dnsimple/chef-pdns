<% node[:pdns][:server].sort.each do |key, value| %>
<% next if value.nil? %>
<%= key.gsub('_', '-') %>=<%=
  case value
  when TrueClass
    'on'
  when FalseClass
    'off'
  when Array
    value.join(',')
  else
    value
  end
%>
<% end %>
